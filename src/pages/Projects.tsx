import React from 'react';
import ProjectsSection from '../components/ProjectsSection';

const Projects: React.FC = () => {
  return (
    <div>
      <ProjectsSection /> {/* Render the Projects Section */}
    </div>
  );
};

export default Projects;

